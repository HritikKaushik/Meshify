{
	"name": "@meshify/root-tests",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Repository-wide test suites (smoke, contracts, integration, e2e). Not published; run by Turbo alongside the per-package suites.",
	"scripts": {
		"test": "vitest run smoke contracts --passWithNoTests",
		"test:integration": "vitest run integration",
		"test:e2e": "vitest run e2e --passWithNoTests"
	},
	"devDependencies": {
		"@meshify/data-access": "workspace:*",
		"@meshify/testing": "workspace:*",
		"@types/pg": "^8.23.1",
		"pg": "^8.23.0",
		"vitest": "^3.2.6"
	}
}
